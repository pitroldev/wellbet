auto-install-peers=true
strict-peer-dependencies=false
dedupe-peer-dependents=true
# node-linker mora no pnpm-workspace.yaml (`nodeLinker: hoisted`). O pnpm 11
# IGNORA `node-linker` aqui no .npmrc — não readicionar nesta linha.
# Segurança de supply-chain (pnpm 11): bloqueia scripts não aprovados.
# Aprovar com: pnpm approve-builds
