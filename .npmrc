auto-install-peers=true
strict-peer-dependencies=false
dedupe-peer-dependents=true
# Expo/React Native exigem hoisting de alguns binários nativos.
node-linker=hoisted
# Segurança de supply-chain (pnpm 11): bloqueia scripts não aprovados.
# Aprovar com: pnpm approve-builds
