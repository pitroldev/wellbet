# Build outputs (node_modules já é ignorado pelo Prettier por padrão)
**/dist/
**/build/
**/.next/
**/.expo/
**/.turbo/
**/coverage/
**/out/

# Saída nativa do Expo CNG / prebuild (gerada — também no .gitignore do app).
# O Prettier usa .prettierignore (não respeita .gitignore), então repetimos aqui.
apps/mobile/android/
apps/mobile/ios/
apps/mobile/expo-env.d.ts

# Código gerado (não editar à mão)
packages/contracts/src/generated/
# Contrato OpenAPI emitido pela api (artefato gerado)
**/openapi.json

# Lockfiles e artefatos
pnpm-lock.yaml
*.tsbuildinfo

# Infra (Terraform/OpenTofu não tem parser no Prettier)
**/.terraform/

# Playground experimental: fica FORA do gate de formatação, igual já está fora
# do gate de ESLint. É um sandbox de design e tem arquivos
# marcados "do not edit" (ex.: lib/brand.ts) que não seguem o estilo do Prettier.
apps/playground/
