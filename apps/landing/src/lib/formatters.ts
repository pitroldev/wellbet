/** Formatadores compartilhados da landing (antes constantes soltas no Hero). */

/** Real brasileiro sem centavos — para stake / valor em jogo. */
export const BRL = {
  style: "currency",
  currency: "BRL",
  maximumFractionDigits: 0,
} as const;
