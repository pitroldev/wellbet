/// <reference types="nativewind/types" />

// Habilita a prop `className` nos componentes RN (NativeWind v4).
// NativeWind cuida APENAS do estilo estático — tudo que se move vai por
// Reanimated 4 / Skia (ver Orçamento de performance, §3).
